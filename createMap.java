import java.util.*;

public class createMap 
{
    // We are creating panels and using a algorithm to pick which panels can be next to each other

    // this class puts the panels together and organizes everything for the panel class. it does not run more than once a run.  
    
}
// 4 seconds 2000 pixels at velocity 8
// thats about 500 pixels a second. so lets go for 15 seconds from spawn in all directions
// thats 7500 pixels each direction or 15000 pixels length. each panel is 2000 pixels so 8 panels width and height

// panel will filter which information is needed for car. car will run the collide methods. 
// we need to determine which panel the car is in and then only run those objects.